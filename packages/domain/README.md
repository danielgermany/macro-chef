# @macro-chef/domain

Shared **pure** TypeScript: formulas, recommendation helpers, validation shared with API.

## Build

```bash
npm run build
```

## Role

Consumable from `@macro-chef/api` (add `"@macro-chef/domain": "*"` workspace dependency when ready). Keep side-effect-free.
