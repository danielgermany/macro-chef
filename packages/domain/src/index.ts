/**
 * Pure domain logic (nutrition math, planning helpers).
 * No HTTP, no database — import from api after building this package.
 */

export function placeholder(): string {
  return 'macro-chef-domain';
}
