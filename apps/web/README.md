# @macro-chef/web

React SPA (Vite + TypeScript).

## Run

```bash
npm run dev
```

Opens `http://localhost:5173`.

## Role

Browser UI for Macro Chef. Will call `apps/api` via `fetch` (TanStack Query later); base URL via `VITE_API_URL` when wired.
