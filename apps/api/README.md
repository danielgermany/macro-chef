# @macro-chef/api

HTTP API (Hono on Node).

## Run

```bash
npm run dev
```

Default port **3000** (override with `PORT`).

## Role

REST + JSON for Macro Chef; will add Zod validation, Postgres, auth middleware, and routes aligned with `docs/FEATURE_INVENTORY.md`.
